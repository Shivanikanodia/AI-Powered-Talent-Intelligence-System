command:
  - streamlit
  - run
  - app.py
  - --server.port
  - "8000"
  - --server.address
  - "0.0.0.0"
# Security: Use Databricks secrets instead of hardcoded tokens
# env:
#   - name: DATABRICKS_TOKEN
#     valueFrom:
#       secretKeyRef:
#         scope: your-secret-scope
#         key: databricks-token
#
# For development: Token will be auto-injected from workspace context